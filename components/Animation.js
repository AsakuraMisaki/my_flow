import { Container } from "pixi.js";
import { Component } from "../core/Entity";

class Animation{
  constructor(textures, animations){
    super();
    this._texs = textures;
    this._animations = animations;
  }

  // scale offset texture ...
  state(frame){
    
  }

}

